function solve(nameCity, populationCity, treasuryCity) {
    let cityRecord = {
        name: nameCity,
        population: populationCity,
        treasury: treasuryCity,

    };

    return cityRecord;

}